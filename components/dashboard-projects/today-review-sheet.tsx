// Removed 2026-05-05.
//
// This file used to export a `TodayReviewSheet` Sheet that opened a side
// panel listing the same 3 projects already visible on the Project Pulse
// dashboard (avatar, health, progress, next tasks). It served no JTBD —
// the Primary action led to a duplicate of the surface beneath it.
//
// The file is intentionally left empty (rather than deleted) to preserve
// the import path in git history. If you find yourself reaching for it,
// stop and ask: what JTBD is the new sheet serving that the dashboard
// cards don't already serve?
export {}
