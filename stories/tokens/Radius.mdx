import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Radius" />

# Radius

A single root token `--radius: 0.625rem` (10px) anchors the scale. Tailwind v4 derives the rest:

```css
@theme inline {
  --radius-sm:  calc(var(--radius) - 4px);  /* 6px  */
  --radius-md:  calc(var(--radius) - 2px);  /* 8px  */
  --radius-lg:  var(--radius);              /* 10px */
  --radius-xl:  calc(var(--radius) + 4px);  /* 14px */
  --radius-2xl: calc(var(--radius) + 8px);  /* 18px */
  --radius-3xl: calc(var(--radius) + 12px); /* 22px */
  --radius-4xl: calc(var(--radius) + 16px); /* 26px */
}
```

## Scale

<div className="grid grid-cols-4 gap-4 not-prose">
  <div className="flex flex-col items-center gap-2">
    <div className="size-20 bg-muted rounded-sm border" />
    <div className="text-xs font-mono">rounded-sm</div>
    <div className="text-xs text-muted-foreground">6px</div>
  </div>
  <div className="flex flex-col items-center gap-2">
    <div className="size-20 bg-muted rounded-md border" />
    <div className="text-xs font-mono">rounded-md</div>
    <div className="text-xs text-muted-foreground">8px</div>
  </div>
  <div className="flex flex-col items-center gap-2">
    <div className="size-20 bg-muted rounded-lg border" />
    <div className="text-xs font-mono">rounded-lg</div>
    <div className="text-xs text-muted-foreground">10px</div>
  </div>
  <div className="flex flex-col items-center gap-2">
    <div className="size-20 bg-muted rounded-xl border" />
    <div className="text-xs font-mono">rounded-xl</div>
    <div className="text-xs text-muted-foreground">14px</div>
  </div>
  <div className="flex flex-col items-center gap-2">
    <div className="size-20 bg-muted rounded-2xl border" />
    <div className="text-xs font-mono">rounded-2xl</div>
    <div className="text-xs text-muted-foreground">18px</div>
  </div>
  <div className="flex flex-col items-center gap-2">
    <div className="size-20 bg-muted rounded-3xl border" />
    <div className="text-xs font-mono">rounded-3xl</div>
    <div className="text-xs text-muted-foreground">22px</div>
  </div>
  <div className="flex flex-col items-center gap-2">
    <div className="size-20 bg-muted rounded-4xl border" />
    <div className="text-xs font-mono">rounded-4xl</div>
    <div className="text-xs text-muted-foreground">26px</div>
  </div>
  <div className="flex flex-col items-center gap-2">
    <div className="size-20 bg-muted rounded-full border" />
    <div className="text-xs font-mono">rounded-full</div>
    <div className="text-xs text-muted-foreground">9999px</div>
  </div>
</div>

## Where each is used

| Class | Where |
|---|---|
| `rounded-sm` | Tooltip arrow, small inline elements |
| `rounded-md` | Buttons, inputs, select triggers, sheet close |
| `rounded-lg` | Tab lists, kanban cards, status pills (`rounded-lg` *or* `rounded-full`), task rows |
| `rounded-xl` | Cards (`Card` from `ui/card.tsx`) |
| `rounded-full` | Avatars, dots, pills, badges (e.g. `HealthBadge`) |

## Rule

**Match the shape of the container.** Don't mix radius sizes across nested components — a card with `rounded-xl` should not contain a button with `rounded-2xl`. The button stays at `rounded-md` because it's a button, not because of its parent.
